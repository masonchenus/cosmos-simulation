# PhysicsEngine.getScaleFactor()

## Overview

Returns the scale factor for visualization.

## Syntax

```javascript
const scale = PhysicsEngine.getScaleFactor();
```

## Returns

- (number): Current scale factor

## Example

```javascript
const scale = PhysicsEngine.getScaleFactor();
```
